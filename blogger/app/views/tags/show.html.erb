<h1>Articles Tagged with <%= @tag.name %></h1>

<ul>
  <% @tag.articles.each do |article| %>  <!-- for each article of our tag -->
    <li><%= link_to article.title, article_path(article) %></li> <!-- show title of article as a link -->
  <% end %>
</ul>
