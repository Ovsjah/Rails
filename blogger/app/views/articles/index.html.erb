<h1>All Articles</h1>

<ul id="articles">
  <% @articles.each do |article| %>
    <li>
      <%= link_to article.title, article_path(article), class: 'article_title', id: "article_#{article.id}" %>  <!-- link_to article.title is the text of the link, article_path(article) is our route (article_path(id) in our case it takes article as parameter gets id from it and build route like this /articles/1)s, class: 'article_title' is our css class for styling links -->
    </li>
  <% end %>
</ul>

<% if logged_in? %>
  <%= link_to "Create a New Article", new_article_path, class: 'new_article' %>
<% end %>
